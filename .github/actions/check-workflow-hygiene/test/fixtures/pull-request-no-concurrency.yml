# A pull-request trigger leaves superseded runs going without a concurrency group.
name: Pull request without concurrency

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: echo build
